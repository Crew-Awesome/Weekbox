{
  "name": "weekbox-root",
  "version": "1.0.0",
  "description": "root de Weekbox",
  "main": "index.js",
  "scripts": {
    "install:all": "npm install && npm --prefix frontend install && npm --prefix extensions/backend install",
    "build:frontend": "npm --prefix frontend run build",
    "watch:frontend": "npm --prefix frontend run build -- --watch",
    "dev:frontend": "npm --prefix frontend run dev",
    "test": "npm --prefix frontend run test",
    "test:watch": "npm --prefix frontend run test:watch",
    "test:notifications": "npm --prefix frontend run test:notifications",
    "start:neu": "neu run",
    "predev:pc": "node -e \"const fs=require('fs'); const c=JSON.parse(fs.readFileSync('neutralino.config.json')); c.url='http://localhost:5173'; fs.writeFileSync('neutralino.config.json', JSON.stringify(c, null, 2));\"",
    "dev:pc": "concurrently \"npm run dev:frontend\" \"neu run\"",
    "dev": "npm run build:frontend && concurrently \"npm run watch:frontend\" \"npm run start:neu\"",
    "prebuild:pc": "node -e \"const fs=require('fs'); const c=JSON.parse(fs.readFileSync('neutralino.config.json')); c.url='/'; c.modes.window.openInspectorOnStartup=false; c.modes.window.enableInspector=false; fs.writeFileSync('neutralino.config.json', JSON.stringify(c, null, 2));\"",
    "format": "node scripts/formatter.js",
    "cap:sync": "npm run build:frontend && npx cap sync android",
    "cap:open": "npx cap open android",
    "cap:run": "node scripts/android-run.js",
    "android:build": "npm run build:frontend && npx cap sync android",
    "android:update": "npm run build:frontend && npx cap sync android",
    "android:run": "npm run build:frontend && npx cap sync android && node scripts/android-run.js"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@capacitor/assets": "^3.0.5",
    "concurrently": "^8.2.2",
    "prettier": "^3.9.6"
  },
  "dependencies": {
    "@capacitor/android": "^8.5.2",
    "@capacitor/app": "^8.0.0",
    "@capacitor/app-launcher": "^8.0.1",
    "@capacitor/cli": "^8.5.0",
    "@capacitor/core": "^8.5.0",
    "@capacitor/filesystem": "^8.1.3",
    "ws": "^8.21.2"
  }
}
